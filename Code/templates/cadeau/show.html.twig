{% extends 'base.html.twig' %}

{% block title %}Cadeau{% endblock %}

{% block body %}
    <h1>Cadeau</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Designation</th>
                <td>{{ cadeau.designation }}</td>
            </tr>
            <tr>
                <th>Age_minimum</th>
                <td>{{ cadeau.ageMinimum }}</td>
            </tr>
            <tr>
                <th>Prix</th>
                <td>{{ cadeau.prix }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('cadeau_index') }}">back to list</a>

    <a href="{{ path('cadeau_edit', {'id': cadeau.id}) }}">edit</a>

    {{ include('cadeau/_delete_form.html.twig') }}
{% endblock %}
